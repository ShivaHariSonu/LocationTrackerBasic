Database used:- ES, postGIS
Have the info for all the police station data:- static
User data:- dynamic(changes while executing the solution)
flag(alert):- to know the status of user(emergency||normal)




Kafka/mqtt(input data:-(userData&& flag))---------->ES||postgis(update userdata constantly in ES||postgis) ---------->(if flag is true:- send message to nearby police coordinate)
